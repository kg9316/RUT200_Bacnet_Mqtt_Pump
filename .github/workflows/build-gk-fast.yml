name: Fast Build GK BACnet MQTT IPKs - TEMP DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Use clean RUT2M cache first
    runs-on: ubuntu-22.04
    steps:
      - name: Stop stale v2 build
        run: |
          echo "Fast Build is temporarily disabled because the previous v2 cache may contain the wrong SDK target."
          echo "Run 'Prepare Clean RUT2M SDK Cache' first, then run 'Full Validate GK BACnet MQTT IPKs - RUT2M'."
          exit 1
